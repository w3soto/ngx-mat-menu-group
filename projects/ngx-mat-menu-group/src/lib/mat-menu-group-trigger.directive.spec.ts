import { NgxMatMenuGroupTrigger } from "./ngx-mat-menu-group-trigger";


describe('NgxMatMenuGroupTrigger', () => {
  it('should create an instance', () => {
    const directive = new NgxMatMenuGroupTrigger();
    expect(directive).toBeTruthy();
  });
});
